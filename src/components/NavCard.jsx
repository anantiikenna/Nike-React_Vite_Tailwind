import { useState } from "react"
import {hamburger} from '../assets/icons'

const NavCard = ({ id, defaultChecked, name }) => {
    const [checked, setChecked ]= useState(defaultChecked = false);
  return (
    
  )
}

export default NavCard